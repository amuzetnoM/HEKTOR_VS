apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vector-studio-data
  namespace: vector-studio
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard  # Change based on your cloud provider
